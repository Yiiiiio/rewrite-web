# Railway 使用 Nixpacks 构建，此文件用于自定义构建过程
[phases.setup]
nixPkgs = ["nodejs-20_x"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = [
  "npm run build",
  "npm run db:generate"
]

[start]
cmd = "npm run start"

